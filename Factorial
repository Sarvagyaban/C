#include <stdio.h>
void main()
{

    int n,f=1;
    printf("Enter the number \n");
    scanf("%d", &n);
    {
        for(int i=1;i<=n;i++)
            f=f*i;
    }
    printf("Factorial of given number is - ");
    printf("%d", f);
}    
